// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    // ইমেজ কনফিগারেশন (যা আপনার ছিল)
    images:{
        unoptimized: true
    },
    // ⬅️ Next.js ডেভেলপমেন্ট ইন্ডিকেটর বন্ধ করার কনফিগারেশন যোগ করুন
    devIndicators: false
};

export default nextConfig;